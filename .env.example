### Copy this file to `.env` and fill values before running `docker compose up --build`
# API keys and secrets (do NOT commit real secrets to repo)
GROQ_API_KEY=
LANGSMITH_API_KEY=

# CORS allow origins (comma-separated). Example: http://localhost:8501
CORS_ALLOW_ORIGINS=http://localhost:8501

# Optional: API URL used by the UI when running outside compose
API_URL=http://localhost:8000
